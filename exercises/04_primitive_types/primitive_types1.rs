// Booleans (`bool`)

fn main() {
    let is_morning = false;
    if is_morning {
        println!("Good morning!");
    }

    
    let is_evening = true;
    if is_evening {
        println!("Good evening!");
    }
}
